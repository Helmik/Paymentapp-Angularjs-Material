'use strict';

describe('Service: miscellaneous/errors/handleerrors', function () {

  // load the service's module
  beforeEach(module('paymentappApp'));

  // instantiate service
  var miscellaneous/errors/handleerrors;
  beforeEach(inject(function (_miscellaneous/errors/handleerrors_) {
    miscellaneous/errors/handleerrors = _miscellaneous/errors/handleerrors_;
  }));

  it('should do something', function () {
    expect(!!miscellaneous/errors/handleerrors).toBe(true);
  });

});
